export interface Product {
  id: string;
  name: string;
  price: number;
  supplierPrice: number;
  stock: number;
  category: string;
  supplierId: string;
  image: string;
  description: string;
}
